<h1>Videos</h1>

<% @videos.each do |video| %>
  <div class="video">
    <%= link_to image_tag(video.thumbnail_url), video %>
    <h2><%= video.title %></h2>
    <p><%= video.description %></p>
    <%= link_to "Watch Video", video %>
  </div>
<% end %>

<%= paginate @videos %>